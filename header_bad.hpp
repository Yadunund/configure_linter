// header without guard

namespace test {

class snake_case_class
{
private:
  int64 _age = 0;
  float64 _height = 0.0;

public:
  snake_case_class(age, height)
  : _age(age),
    _height(height)
  {
  }

  int get_age();
};

}

// namespace closed without comment
